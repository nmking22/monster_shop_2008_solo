<h1>All Users</h1>

<% @users.each do |user| %>
  <section id = "user-<%= user.id %>">
    <%= link_to "#{user.name}", "/admin/users/#{user.id}" %>
    <p>Created at: <%= user.created_at %></p>
    <p>Role: <%= user.role %></p>
  </section>
<% end %>
